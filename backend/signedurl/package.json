{
  "name": "getsignedurl-service",
  "version": "1.0.0",
  "description": "Cloud Run service that generates (resumable or simple) signed URLs for uploading recordings to GCS.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@google-cloud/storage": "^7.10.0",
    "express": "^4.19.2",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18"
  }
}
